use std::str::FromStr;
use crate::objects::Object;
use crate::gc::Gc;

grammar(gc: &mut Box<Gc>);

//Num: i32 = <s:r"[0-9]+"> => i32::from_str(s).unwrap();

// 7.1.2. External representations
pub Datum: Object = {
    SimpleDatum,
}

pub SimpleDatum: Object = {
    Boolean,
}

// R7RS small 7.1.1. Lexical structure.
Boolean: Object = {
    "#t" => Object::True,
    "#true" => Object::True,
    "#f" => Object::False,
    "#false" => Object::False,
}

